<resources xmlns:tools="http://schemas.android.com/tools">

    <!--SplashScream -->
    <style name="Theme.splashScreen" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_img_dark</item>
        <item name="android:statusBarColor">@color/verde_base</item>
    </style>

    <!-- Base application theme. -->
    <style name="Theme.CovertPots" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/verde_base</item>
        <item name="colorPrimaryVariant">@color/rojo</item>
        <item name="colorOnPrimary">@color/negro</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/verde_base</item>
        <item name="colorSecondaryVariant">@color/rosado</item>
        <item name="colorOnSecondary">@color/blanco</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/verde_base</item>
        <!-- Customize your theme here. -->
    </style>
</resources>